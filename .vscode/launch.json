{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev",
            "request": "launch",
            "command": "yarn dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
